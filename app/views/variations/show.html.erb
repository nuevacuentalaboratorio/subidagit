<h1>Item in <%= @product.title %></h1>

<p>
  <b>variation name:</b>
  <%= @variation.name %>
</p>


<%= link_to 'Edit', edit_product_variation_path(@product, @variation) %> |
<%= link_to 'Back', product_variations_path(@product) %>